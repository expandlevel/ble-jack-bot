name: Run

on: [push]

jobs:
  run-external-bot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: oven-sh/setup-bun@v2

      - name: Install bun dependencies
        run: bun install

      - name: Run External Bot
        run: bun run start:external
